# TODO: Fix Jest Test Error

- [x] Add missing Jest dependencies to package.json (jest, ts-jest, @types/jest, mongodb-memory-server)
- [ ] Run npm install to install the dependencies
- [ ] Run npm test to verify the fix
